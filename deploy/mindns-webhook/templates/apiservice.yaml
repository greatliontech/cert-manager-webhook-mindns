apiVersion: apiregistration.k8s.io/v1
kind: APIService
metadata:
  name: v1alpha1.{{ include "mindns-webhook.groupName" . }}
  labels:
    app: {{ include "mindns-webhook.name" . }}
    chart: {{ include "mindns-webhook.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
  annotations:
    cert-manager.io/inject-ca-from: "{{ .Release.Namespace }}/{{ include "mindns-webhook.servingCertificate" . }}"
spec:
  group: {{ include "mindns-webhook.groupName" . }}
  groupPriorityMinimum: 1000
  versionPriority: 15
  service:
    name: {{ include "mindns-webhook.fullname" . }}
    namespace: {{ .Release.Namespace }}
  version: v1alpha1
